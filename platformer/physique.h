#pragma once
static sf::Vector2f player_spawn(80, 450);
static sf::Vector2f player_pos(player_spawn);
static sf::Vector2f player_vel(0, 0);
static bool grounded;
const sf::Vector2f player_size(20, 24);